# Force LF line endings for all text files on all platforms.
# This prevents Windows CRLF from breaking shell scripts on the Pi.
* text=auto eol=lf

# Explicitly mark shell scripts
*.sh text eol=lf

# Explicitly mark markdown
*.md text eol=lf

# YAML (CI workflows)
*.yml text eol=lf
*.yaml text eol=lf
