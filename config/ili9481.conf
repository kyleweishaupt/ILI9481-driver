# ILI9481 Userspace Framebuffer Daemon — Configuration
#
# INI-style key=value format.
# Lines starting with # or ; are comments.
# Unknown keys are silently ignored.

[display]
# Display rotation: 0, 90, 180, 270
rotation = 270

# Target refresh rate in frames per second (1–60)
fps = 30

# Framebuffer device to mmap and mirror to the display
fb_device = /dev/fb1

[touch]
# Enable XPT2046 touch support (0 = disabled, 1 = enabled)
# WARNING: GPIO 7 (DB0) = SPI CE1 and GPIO 8 (DB1) = SPI CE0 conflict
# with the parallel data bus.  Touch is OFF by default.
enable_touch = 0

# SPI device for the XPT2046 touch controller
spi_device = /dev/spidev0.1

# SPI clock speed in Hz
spi_speed = 2000000
